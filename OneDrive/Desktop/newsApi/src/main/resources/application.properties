# Spring Boot application name
spring.application.name=time-scraper

# Server configuration
server.port=8080

# Logging configuration (adjust as needed)
logging.level.root=INFO

# Other application-specific properties can be added here
# For example, database connection settings, security settings, etc